DATABASE_DSN=postgres://postgresadmin:postgresadmin@localhost:5431/customers
RABBIT_DSN=amqp://guest:guest@localhost:5672
ORDERS_URL=http://localhost:8082

# Keycloak JWT Authentication
# Option 1: Use JWKS endpoint (recommended for production)
KEYCLOAK_REALM_URL=http://localhost:8080/realms/payetonkawa

# Option 2: Use public key directly (for testing or when JWKS not available)
# KEYCLOAK_PUBLIC_KEY=-----BEGIN PUBLIC KEY-----\nMIIBIjANBgkqhkiG9w0BAQEFAAOCAQ8AMIIBCgKCAQEA...\n-----END PUBLIC KEY-----

# Authentication Debug Mode (set to "true" or "1" to enable verbose logging)
# WARNING: Disable in production as it reveals sensitive information
# AUTH_DEBUG=false
